{
  "name": "terms-lens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Evidence-first Chrome extension that explains website terms in plain English.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check server/analyzer.js && node --check extension/service-worker.js && node --check extension/content-script.js && node --check extension/sidepanel.js"
  },
  "engines": {
    "node": ">=20"
  }
}
